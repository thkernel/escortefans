<%= form_with(model: language_item, local: true) do |form| %>
  <% if language_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(language_item.errors.count, "error") %> prohibited this language_item from being saved:</h2>

      <ul>
      <% language_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="field">
    <%= form.label :language_id %>
    <%= form.text_field :language_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
