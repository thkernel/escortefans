<%= form_with(model: astrology, local: true) do |form| %>
  <% if astrology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(astrology.errors.count, "error") %> prohibited this astrology from being saved:</h2>

      <ul>
      <% astrology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
