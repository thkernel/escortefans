<p id="notice"><%= notice %></p>

<h1>Physical Appearances</h1>

<table>
  <thead>
    <tr>
      <th>Uid</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @physical_appearances.each do |physical_appearance| %>
      <tr>
        <td><%= physical_appearance.uid %></td>
        <td><%= physical_appearance.user %></td>
        <td><%= link_to 'Show', physical_appearance %></td>
        <td><%= link_to 'Edit', edit_physical_appearance_path(physical_appearance) %></td>
        <td><%= link_to 'Destroy', physical_appearance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Physical Appearance', new_physical_appearance_path %>
